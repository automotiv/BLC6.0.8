language: java

jdk: openjdk8

build:
  ci:
    - sdk use maven 3.6.3
    - mvn clean
    - mvn install
    - cd site
    - mvn clean
    - mvn install
    - cd ..
    - cd admin
    - mvn clean
    - mvn install
    - cd ..
    - cd api
    - mvn clean
    - mvn install
